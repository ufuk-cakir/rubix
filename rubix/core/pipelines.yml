calc_ifu:
  Transformers:
    rotate_galaxy:
      name: rotate_galaxy
      depends_on: null
      args: []
      kwargs:
        type: "face-on"
    # This does not work inside the pipeline since we need dynamic JAX arrays here, and its not suppoerted for jit
    # filter_particles:
    #   name: filter_particles
    #   depends_on: rotate_galaxy
    #   args: []
    #   kwargs: {}
    spaxel_assignment:
      name: spaxel_assignment
      depends_on: rotate_galaxy
      args: []
      kwargs: {}

      # split_data: Same: I dont think this works
    #   name: split_data
    #   depends_on: spaxel_assignment
    #   args: []
    #   kwargs: {}
